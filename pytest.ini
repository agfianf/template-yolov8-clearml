[pytest]
env =
    CVAT_USERNAME=""
    CVAT_PASSWORD=""
    CVAT_ORGANIZATION=""
    CVAT_FORMAT_DATA=
    CVAT_HOST=
    CVAT_OUTPUT_DIR=

addopts = -p no:warnings

markers =
    slow: invokes a real exporter or model build; a few seconds each